# Daily Note - 2026-05-04

- `focus_of_day`: foundational architecture and simulation demo workflow

## Work Done

1. Minimal microgrid state schema and first simple simulation.
2. `window1` baseline setup for:
   - load,
   - solar,
   - battery (minimal).
3. Added `Run Simulation` button with simple output plot.
4. Introduced stores and callbacks in cleaner structure.
5. Planned metrics-table path (`core -> application -> ui`).
6. Introduced gymnasium direction with a microgrid gym environment.
7. Defined next core extension direction: hydrogen system (electrolyser, tank, fuel cell).

## Architecture Understanding (evolving)

- Clarified need to separate:
  - presentation (`ui_dash`),
  - application orchestration (`application/use_cases`),
  - infrastructure concerns (serialization/storage),
  - core simulation logic.

## Next Steps (discussion and execution)

8. Demonstrate electrolyser dynamics and hydrogen production simulation in `window2`.
   - rename `window1` -> demo microgrid
   - rename `window2` -> demo hydrogen production
9. Define and implement first industrial use case incrementally.
