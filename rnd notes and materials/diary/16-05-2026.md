# Daily Note - 2026-05-16

- `focus_of_day`: incremental UI slice for forecast-based H₂ control on synthetic data

## Planned Tasks

1. Add a UI-selectable control mode for the existing battery baseline vs forecast H₂ use case.
2. Reuse synthetic solar/load samples so the new scenario can be run without external data.
3. Expose first forecast/H₂ parameters in the setup panel.
4. Update documentation and prepare a progress post draft.

## Work Done

- Added `Forecast H₂ control` as a selectable mode in `window1`.
- Added first UI controls for forecast model type, horizon/window, tank capacity, initial tank level, and electrolyser max current.
- Wired the Dash callback to call `run_forecast_h2_offgrid_from_ui_battery` when forecast H₂ mode is selected.
- Converted synthetic irradiance to effective solar kW using the current `Solar` default PV area of 100 m² so one synthetic profile supports both baseline and H₂ runs.
- Extended result plotting to include electrolyser power and hydrogen tank level when those telemetry columns exist.
- Added scalar KPI metrics for the forecast H₂ use case.
- Fixed minimal-environment execution by making `tqdm` usage graceful and adding it to project dependencies.
- Stabilized electrolyser initialization/dynamics so the H₂ use case can run on current pandas versions.

## Decisions / Assumptions

- Keep the first UI integration in `window1` instead of adding a separate window. This preserves an incremental workflow and lets users compare the baseline and forecast H₂ mode from one panel.
- Use synthetic data only for this slice; trained forecaster artifacts remain a later infrastructure step.
- Default electrolyser current is conservative (`4 A`) to keep the demo tank within bounds on multi-day synthetic runs.

## Validation

- Ran the application forecast use case on the default synthetic 5-day profile.
- Confirmed KPI output includes energy totals, H₂ production, final tank level, final battery SOC, and electrolyser degradation proxy.

## Next Immediate Step

- Add UI affordances for comparing oracle vs rolling forecast runs side-by-side.
- Add curtailment/residual-energy KPIs once the power-balance model explicitly tracks curtailed energy.
