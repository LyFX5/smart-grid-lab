# Daily Note - 2026-05-14

- `focus_of_day`: define forecast-based H₂ control use case (off-grid) + Phase A scaffolding

## Planned Tasks

1. Architectural inspection (layers, gaps in core coupling).
2. Author use case specification under `rnd notes and materials/use_cases/`.
3. Add application modules: forecasting heuristics, H₂ set-point policy, Phase A runner.
4. Update roadmap, decisions, experiment log.

## Work Done

- Drafted **UC-OFFGRID-H2-FORECAST-CONTROL** (phases A–D) with architecture table and KPIs (updated after layering review).
- Implemented hydrogen + forecast on the **core engine**:
  - `core/forecasting/` — `SurplusForecastModel` + oracle / rolling heuristics,
  - `core/controllers/` — `ForecastElectrolyserBatteryStrategy`, `h2_surplus_policy`,
  - `core/microgrid.py` — ordered step + tank mass balance from electrolyser,
  - `Simulation(setup, strategy=...)` injection,
  - application use case `run_forecast_h2_offgrid_simulation` (wires only).
- Infrastructure stub: `SklearnSurplusForecastAdapter`.
- Populated `core/__init__.py` exports (was empty; fixed broken imports).
- Cleaned dead commented block from `demo_simulation.py` (duplicate `SimulationSetup` / `run_simulation`).

## Decisions / Assumptions

- Surplus for forecasting = **solar electrical power − load** (kW), aligned to `Time.index_range`.
- Electrolyser scheduled from forecast; **battery** receives **residual** net minus electrolyser electrical draw.
- Oracle forecast used by default for MPC-style debugging; rolling backward mean available via config flag.

## Risks / Blockers

- `Solar.pv_plant_area` is still hardcoded (100 m²); use case maps kW→irradiance with the same constant until configurable.

## Next Immediate Step

- Wire **window2** to `run_forecast_h2_offgrid_simulation` (thin callback).
- Implement real inference in `SklearnSurplusForecastAdapter` when a model artifact exists.

## 2026-05-14 (evening) — architecture correction

- Re-aligned with **your** layering: **all components simulatable in core**; forecast **contract** in core; **inference** in infrastructure; **application** composes concrete forecast + strategy and runs **`Simulation` only**.
- Removed duplicate application-only forecasting packages; hydrogen stepping integrated in `Microgrid`.
